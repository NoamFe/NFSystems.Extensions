# NFSystems.Extensions
examples of usage:

1)Create random enum (In this example, CarSize is an enum):
```
EnumExtensions.Random<CarSize>()
```

2)check if a collection is empty:
```
_vehiclesIn.IsEmpty()
```
